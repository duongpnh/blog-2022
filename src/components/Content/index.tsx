import * as React from "react";

import Heading from '../Heading';
import DataDisplay from '../DataDisplay';

const index = () => (
  <div className="h-screen w-full bg-white mt-px box-border">
    <Heading />
    <DataDisplay />
  </div>
)

export default index